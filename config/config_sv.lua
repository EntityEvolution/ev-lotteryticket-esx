Config = Config or {}
Config.setLang = 'en' -- Select the language the mnu is going to be on (en, es, fr). 

Config.useESX = false -- Only set to true if using ESX older than legacy, else go to the fxmanifest

Config.kickPlayer = false
Config.kickMessage = "I can't believe you're dumb enough to believe in a fake event"